// preload.js
// This file runs in the renderer process before your React bundle loads.
window.addEventListener('DOMContentLoaded', () => {
  // You can expose safe APIs here via contextBridge if needed.
});
